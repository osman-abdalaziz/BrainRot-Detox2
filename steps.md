المرحلة الأولى: البنية التحتية والترحيل (Database & Migration)

لا يمكننا بناء منطق جديد على هيكل بيانات قديم.

    إنشاء عداد النظام (System Config): سننشئ مستنداً في Firestore configs/system لتتبع رقم الدورة الحالية (currentCycle).

    سكريبت الترحيل (Migration Script): سنكتب كوداً في لوحة الإدارة admin.js يمر على جميع المستخدمين الحاليين ويضيف لهم الحقول الجديدة دون مساس ببياناتهم القديمة:

        currentZone (لإدارة الحالات: أخضر، أصفر، أحمر).

        cycleScore (نقاط الدورة الأسبوعية التنافسية).

        earnedStreakBadges (مصفوفة لمنع تكرار أوسمة الستريك).

        badges (مصفوفة لتخزين الأوسمة المرئية).

المرحلة الثانية: القاضي الآلي (Cloud Functions)

هذا هو عصب النظام الجديد، وسنكتبه في ملف functions/index.js ليعمل تلقائياً فجر كل سبت (الساعة 12:01 ص).

    المنقذ الذكي السحابي (Global Smart Savior): فحص إجباري لبيانات يوم الجمعة لكل مستخدم، ومحاسبة المتخلفين عن الأساسيات الدينية أو أهداف النقاط.

    محرك الأوسمة الفردية (Streak Badges): فحص الستريك المحدث ومنح أوسمة (7، 14، 21...) وتخزينها في قاعدة البيانات.

    التتويج الأسبوعي وتصفير الدورة (The Wipe): ترتيب المستخدمين حسب الـ cycleScore، منح أوسمة الـ Top 3 باسم الدورة الحالية، ثم تصفير cycleScore للجميع وزيادة رقم currentCycle بواحد.

المرحلة الثالثة: محرك الحالات (Frontend Zone Engine)

هنا سنقوم بتعديل ملف dashboard.js ليتماشى مع النظام الجديد يومياً.

    تعديل زر الاعتماد والمنقذ الذكي المحلي: ربط النجاح والفشل بالانتقال بين المناطق (Green -> Yellow -> Red).

    تطبيق العقوبات البصرية (UI Friction): كتابة أكواد الـ JavaScript والـ CSS التي تحول الشاشة للون الرمادي في "المنطقة الحمراء"، وتغلق المتجر، وتخفي المتصدرين.

    مهام الكفارة (Restitution Tasks): برمجة آلية خروج المنتكس من المنطقة الحمراء عبر إثبات التعافي للذكاء الاصطناعي أو الأصدقاء.

المرحلة الرابعة: لوحة الصدارة وعرض الأوسمة (Leaderboard & Badges UI)

    تعديل لوحة الصدارة (Leaderboard): جعلها ترتب المستخدمين بناءً على cycleScore الأسبوعي بدلاً من النقاط التراكمية، لضمان العدل للوافدين الجدد.

    برمجة واجهة الأوسمة: تعديل الـ HTML والـ JS لجلب مصفوفة badges وعرض الأوسمة (بصورها وعناوينها وتواريخها) في الملف الشخصي للمستخدم.

المرحلة الخامسة: صمامات الأمان (Edge Cases & Final Polish)

    أداة التجميد (Freeze Ticket): تعديل منطقها لتعمل كدرع يحمي من السقوط للمنطقة الصفراء أو كسر الستريك.

    نظام النزيف (Active Bleed): تفعيل الخصم اليومي من النقاط التراكمية (lifetimeScore) لمن يقبع في المنطقة الحمراء بدون محاولة للتعافي.

هذه هي الخطة محبكة ومقسمة لـ 5 مراحل.
هل توافق على هذا التسلسل؟ إذا كانت الإجابة نعم، أعطني الأمر لنبدأ بكتابة كود المرحلة الأولى (سكريبت الترحيل وإعداد قاعدة البيانات).
