# خطة تطوير BrainRot Detox - الإصدار الثاني (V2)

## 1. نظام التحديث النووي (Nuclear Reload)

- [ ] تعديل `sw.js`: إجبار الـ Service Worker على مسح الكاش بالكامل من الجذور عند تغيير رقم الإصدار.
- [ ] تعديل `dashboard.js`: زرع متتبع (Listener) يلتقط التحديث الجديد ويجبر المتصفح على عمل `window.location.reload(true)` لمسح الكاش الحي من الذاكرة وتخطي عناد أجهزة الأندرويد.

## 2. نظام المستويات العسكرية (Progression System)

تقسيم المستخدمين إلى 3 رتب بناءً على `lifetimeScore` (المستوى تصاعدي فقط):

- [ ] **المستوى المبتدئ (0 إلى 1000 XP):**
    - العقوبة العادية (الفشل): كسر الستريك فقط. لا خصم عملات، ولا طرد للمنطقة الصفراء/الحمراء.
    - الفشل الكارثي (تجاوز الحد الأقصى للوقت): كسر الستريك + تصفير نقاط المهام لذلك اليوم (0 XP).

- [ ] **المستوى المتوسط (1001 إلى 5000 XP):**
    - العقوبة: كسر الستريك + الطرد للمناطق + خصم **نصف** قيمة الهدف اليومي من العملات.

- [ ] **المستوى المحترف (+5000 XP):**
    - العقوبة: كسر الستريك + الطرد للمناطق + خصم **كامل** قيمة الهدف اليومي من العملات.

## 3. محرك تقييم الضمير (The Honor System)

إلغاء الذكاء الاصطناعي والصور، والانتقال للتقييم اليدوي بمعادلات تناسبية.

- [ ] **تصميم الواجهة (Time Picker):** - نافذة تحتوي على 3 بكرات إدخال أصلية (Native Time Input) لـ: وقت الشاشة الإجمالي، وقت الدراسة/العمل، وقت الشورتس.
- [ ] **المعادلة الرياضية (150 نقطة مقسمة):**
    - الوقت المهدر = الإجمالي - وقت الدراسة.
    - `نقاط المهدر = 75 * (1 - (الوقت المهدر الفعلي / maxWastedTime))`
    - `نقاط الشورتس = 75 * (1 - (وقت الشورتس الفعلي / maxShortsTime))`
    - _ملاحظة:_ أي ناتج سالب يُرفع إلى الصفر.
- [ ] **التقييم النهائي:** - `المجموع الكلي = نقاط المهام + نقاط المهدر + نقاط الشورتس`.
    - إذا كان المجموع الكلي >= الهدف اليومي (ينجح). أقل من الهدف (يفشل وتُطبق عقوبة المستوى).

## 4. الإدارة الديناميكية من السيرفر (Admin Control)

لا توجد أرقام ثابتة (Hardcoded) في الكود، كل شيء يدار من قاعدة البيانات.

- [ ] إنشاء مستند `systemSettings/levels` في Firebase يحتوي على `maxWastedTime` و `maxShortsTime` لكل مستوى بشكل مستقل.
- [ ] جعل دوال التقييم (سواء الاعتماد الحي أو المنقذ الذكي) تسحب هذه القيم قبل حساب النقاط أو العقوبات.

## 5. نظام المهام الطبقية (Level-Specific Tasks)

- [ ] **تعديل قاعدة البيانات:** إضافة مصفوفة `targetLevels` لكل مهمة (مثال: `['beginner', 'intermediate']`).
- [ ] **لوحة التحكم:** إضافة صناديق اختيار (Checkboxes) عند إضافة/تعديل أي مهمة لتحديد من يحق له رؤيتها.
- [ ] **واجهة المستخدم:** فلترة المهام المعروضة في `dashboard.js` بناءً على رتبة المستخدم الحالية لضمان عدم رؤيته لمهام لا تخص مستواه.
